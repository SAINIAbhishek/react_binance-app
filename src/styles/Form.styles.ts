import styled from "styled-components";

export const Select = styled.select`
  width: 200px;
  font-weight: bold;
  padding: 12px;
  border-radius: 4px;
  font-size: 14px;
  cursor: inherit;
  line-height: inherit;
`;
